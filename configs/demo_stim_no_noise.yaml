experiment:
  name: demo_stim_no_noise
  description: "Real Stim circuit, Stim simulator, provided syndrome extraction, no noise."
  seed: 1

code:
  family: surface_code
  distance: 3
  rounds: 1
  basis: both
  reset_mode: reset

backend:
  name: simulator
  shots: 32
  options:
    seed: 1

noise:
  model: no_noise
  parameters: {}

decoder:
  name: observable_rate
  options: {}

mapping:
  strategy: none
  hardware_patch: null

artifacts:
  root: results
  save_raw_measurements: true
  save_syndromes: true
  save_report: true
